import { useState } from 'react'
import { Mail, Lock, Eye, EyeOff } from 'lucide-react'
import { useNav } from '../context/NavContext'
import { StatusBar } from '../components/shared'
import logo from '../assets/helpy-logo.jpeg'
export default function LoginPage(){const{navigate}=useNav();const[showPw,setShowPw]=useState(false);const[email,setEmail]=useState('');const[pw,setPw]=useState('');return <div className="flex flex-1 flex-col overflow-hidden bg-[#EEF3FF]"><StatusBar/><div className="flex-1 overflow-y-auto px-6 pb-10"><div className="flex flex-col items-center pt-10"><img src={logo} className="mb-4 h-24 w-24 rounded-3xl object-cover shadow-lg"/><h1 className="text-3xl font-black text-gray-900">Welcome to Helpy</h1><p className="mt-2 text-center text-sm leading-6 text-gray-500">Sign in and discover premium services across Qatar</p></div><div className="mt-10 rounded-[32px] bg-white p-5 shadow-xl"><div className="space-y-4"><div className="flex items-center gap-3 rounded-2xl border border-gray-200 px-4 py-4"><Mail size={18} className="text-gray-400"/><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email or phone number" className="flex-1 bg-transparent text-sm outline-none"/></div><div className="flex items-center gap-3 rounded-2xl border border-gray-200 px-4 py-4"><Lock size={18} className="text-gray-400"/><input type={showPw?'text':'password'} value={pw} onChange={e=>setPw(e.target.value)} placeholder="Password" className="flex-1 bg-transparent text-sm outline-none"/><button onClick={()=>setShowPw(!showPw)}>{showPw?<EyeOff size={18}/>:<Eye size={18}/>}</button></div><div className="text-right text-sm font-semibold text-brand-500">Forgot password?</div><button onClick={()=>navigate('verify')} className="w-full rounded-2xl bg-brand-500 py-4 text-sm font-bold text-white shadow-lg">Sign In</button></div></div></div></div>}
